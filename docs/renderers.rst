Renderers
=========

What is it?
-----------

A renderer converts your icon definition to HTML. This package includes a `IconRenderer` which renders to a single HTML tag with CSS classes. You can use a customized renderer to support specific icon libraries or code.

.. automodule:: django_icons.renderers
