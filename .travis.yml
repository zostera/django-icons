dist: bionic

language: python

python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9-dev"

before_install: pip install poetry

install: pip install tox-travis coveralls

script:
  - tox

after_success:
  - coverage combine
  - coveralls
